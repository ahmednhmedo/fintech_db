# HOST Python deps (Windows). Install into a Python 3.11 venv used by run_gold.ps1.
# These run the silver->SQL Server loader and dbt against ahmed\SQLEXPRESS (Windows auth).
dbt-core==1.8.7
dbt-sqlserver==1.8.4     # local SQL Server target via ODBC Driver 18; windows_login auth
dbt-fabric==1.8.7        # pinned: dbt-sqlserver 1.8.4 needs <1.8.8
pyodbc==5.1.0            # ODBC for the loader + dbt-sqlserver
pyarrow==16.1.0          # read silver Parquet
pandas==2.2.2
sqlalchemy==2.0.30
