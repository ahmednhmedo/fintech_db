# Streamed Bronze / DLQ landings are regenerated data — never commit them.
bronze/*.jsonl
dlq/*.jsonl

# Silver incremental outputs + offset checkpoint (regenerated by silver_incremental.py).
silver/
quarantine/
_checkpoints/
